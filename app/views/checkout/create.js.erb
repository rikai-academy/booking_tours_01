var stripe = Stripe("<%= Rails.configuration.stripe[:publishable_key] %>");

stripe.redirectToCheckout({
  sessionId: "<%= @session.id %>"
}).then(function (result) {
  alert("<%= t('bookings.shared.failed') %>")
  console.log(result.error.message);
});