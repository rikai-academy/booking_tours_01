<% provide(:title, t("review.review.title")) %>
<div class="container">
  <div class="row">
    <h3><%=t("review.review.allreview")%></h3>
  </div>
  <div class=row>
    <%= link_to t('review.new.create'), new_review_path, {class:"btn btn-info mr-auto"} %>
    <%= paginate @reviews, theme: "twitter-bootstrap-4" %>
  </div>
  <div class="row mt-4">
    <%= render @reviews%>
  </div>
</div>